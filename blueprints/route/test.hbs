import { module, test } from 'qunit';
import { setupTest } from '@/tests/helpers';

module('Unit | Route | {{pascalCase name}}', function (hooks) {
  setupTest(hooks);

  test('it exists', function (assert) {
    let route = this.owner.lookup('route:{{dasherize name}}');
    assert.ok(route);
  });
});
