import { module, test } from 'qunit';
import { setupTest } from '@/tests/helpers';

module('Unit | Service | {{pascalCase name}}', function (hooks) {
  setupTest(hooks);

  // TODO: Replace this with your real tests.
  test('it exists', function (assert) {
    let service = this.owner.lookup('service:{{dasherize name}}');
    assert.ok(service);
  });
});
